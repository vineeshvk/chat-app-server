# import Error from '../../config/sharedSchema.graphql'
# import Error from '../../config/sharedSchema.graphql'

type Query {
	getChats(userId: String): [Chat]
}

type Mutation {
	createChat(membersId: [String]): [Error!]
}
